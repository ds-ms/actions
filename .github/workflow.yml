on: push

jobs:
  build:
    runs-on:
      pool: default
    actions:
      - run: echo hi
      - run: printenv
      - uses: ./k8s-login
        with:
          kubeconfig: someKubeConfig
        id: login
      - run: cat $KUBECONFIG
