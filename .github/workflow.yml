on: push

jobs:
  build:
    runs-on:
      pool: default
    actions:
      - run: echo hi
      - uses: ./login
        with:
          kubeconfig: someKubeConfig
        id: login
      - run: printenv
      - uses: ./logout
        id: logout
      - run: printenv
